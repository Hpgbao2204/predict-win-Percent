# YOLO Dataset Configuration
# Champion Card Detection Dataset

# Dataset paths
path: /home/hpgbao2204/Downloads/predict-win-Percent/dataset
train: images  # train images (relative to 'path')
val: images    # val images (relative to 'path') - will use same as train for now

# Classes
names:
  0: champion_card  # Only one class - all champion cards are same class

# Number of classes
nc: 1